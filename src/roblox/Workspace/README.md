# Workspace Layer

Place-authored objects or source-controlled workspace configuration should be rooted here.

Use this directory only for data-model content that truly belongs in `Workspace`, not for shared
runtime logic that should live in `ReplicatedStorage` or `ServerScriptService`.
